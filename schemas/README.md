# schemas/

JSON schemas for the Agent Guides spec — `SKILL.md` and `GUIDE.md` front matter, step files, manifests.

Each schema is reachable at `https://agentguides.io/schemas/<name>.json` and versioned by its `$id`.
