# System dependencies not managed by mise. Install with: brew bundle
# mise then provisions the rest (ruby, node, just, lefthook, gh, markdownlint-cli2)
# from mise.toml via `mise install`.
brew "mise"
brew "librsvg" # provides rsvg-convert, used by `just brand`
